type Query {
  me: User
}

type Mutation {
  refreshToken: Boolean
  login(email: String!, password: String!): Boolean
  register: Boolean
}

type User {
  id: Int
  username: String!
  email: String!
  password: String
  createdAt: String
  updatedAt: String
}
