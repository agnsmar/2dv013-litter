query Profile($userid: String!) {
  profile(userid: $userid) {
    error {
      message
    }
    profile {
      avatar
      content
      lits {
        content
        created_at
        updated_at
      }
      username
    }
  }
}