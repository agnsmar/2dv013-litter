type Query {
  feed(offset: Int!, take: Int!): [Feed]
}

type Mutation {
  addLit(content: String!): Boolean
}

type Feed {
  user_id: Int!
  username: String!
  content: String!
  createdAt: String!
  updatedAt: String
}