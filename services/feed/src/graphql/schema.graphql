type Query {
  feed(offset: Int!, take: Int!): [Feed]
}

type Mutation {
  addLit(content: String!): Boolean
}

type Feed {
  userId: Int!
  username: String!
  content: String!
  createdAt: String!
  updatedAt: String
}