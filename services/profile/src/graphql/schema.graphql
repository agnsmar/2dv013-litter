type Query {
  profile(userid: String!): ProfileRespone
}

type ProfileError {
  message: String!
}

type ProfileRespone {
  profile: Profile
  error: ProfileError
}

type Profile {
  username: String!
  content: String
  avatar: String!
  lits: [Lit]
}

type Lit {
  content: String!
  createdAt: String!
  updatedAt: String!
}